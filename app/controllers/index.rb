get '/' do
	erb :index
end

post '/login' do
#   @user = User.find_by(:email params[:email])
#   if @user.authenticate(params[:password])
#     session[:user_id] = user.id
#	  redirect to secret place
#   else
#     redirect to home_url
#   end
end

delete '/' do

end